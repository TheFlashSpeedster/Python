# start = initialize, default = 0
# stop = termination condition, main
# step = loop variable increment, optional

# Range = [start, stop)
# Range with step = [start, stop, step)

for i in range(1,11): # Prints 10 times
    print(i, "Hello World!")